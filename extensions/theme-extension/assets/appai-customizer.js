/**
 * AppAI Customizer Block — Disabled stub
 * ─────────────────────────────────────────────────────────────────────────
 * This block has been superseded by the App Embed (ai-art-embed.liquid),
 * which handles all customizer page rendering through the unified
 * /embed/design iframe.
 *
 * This script intentionally does nothing. The root element
 * (#appai-customizer-root) from ai-art-customizer.liquid is hidden.
 */
;(function () {
  'use strict';
  var root = document.getElementById('appai-customizer-root');
  if (root) {
    root.style.display = 'none';
  }
})();
